1. Run "Update-Database" in the Package Manager Console (assuming having SQL Server installed with 1433 port open).
2. Right clik on the project -> Properties, go to Signing and then Create Test Certificate
